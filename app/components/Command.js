/**
  Command.js
  Class stub for all dump-tool plugin commands. A command should represent one
  instance of a user entered command in the application
**/

class Command {
  hook(vorpal, options = {}) {}
}

module.exports = Command;
